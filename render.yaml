services:
  - type: web
    name: slolonest
    plan: free
    runtime: python
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate && python create_superuser.py
    startCommand: gunicorn soloNest.wsgi:application --log-file -
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.8"
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DJANGO_DEBUG
        value: "False"
      - key: DJANGO_ALLOWED_HOSTS
        value: "slolonest.onrender.com,localhost,127.0.0.1"
      - key: DATABASE_URL
        fromDatabase:
          name: slolonest-db
          property: connectionString
      - key: RAZORPAY_KEY_ID
        sync: false
      - key: RAZORPAY_KEY_SECRET
        sync: false
      - key: EMAIL_BACKEND
        value: "django.core.mail.backends.console.EmailBackend"
      - key: DEFAULT_FROM_EMAIL
        value: "SoloNest <noreply@slolonest.onrender.com>"
      - key: OTP_DEV_MODE
        value: "False"
      - key: CSRF_TRUSTED_ORIGINS
        value: "https://slolonest.onrender.com"

databases:
  - name: slolonest-db
    databaseName: slolonest
    plan: free
